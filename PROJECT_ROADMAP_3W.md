# AI 应用工程师 30K 级个人项目年度路线图

> 版本：v1.1
> 制定日期：2026-06-29
> 最近修订：2026-06-29（项目一调整为招投标智能审查平台）
> 适用目标：工作一年后应聘 AI 应用工程师、AI Agent 工程师、Python AI 后端工程师。
> 核心作品：① 招投标文件智能审查与响应辅助平台；② 软件需求与代码质量 Agent。

---

## 1. 这份计划如何使用

这不是一份“照着教程做两个 Demo”的清单，而是一份以求职证据为导向的工程计划。两个项目最终必须同时证明：

1. 能独立完成需求分析、架构设计、编码、测试、部署和迭代。
2. 理解 RAG、Agent、Tool Calling、状态管理和评测，而不只是调用模型 API。
3. 能处理超时、并发、权限、恢复、成本、安全和可观测性等生产问题。
4. 能通过基准测试、对照实验和失败案例证明技术选择的价值。
5. 能在 5 分钟内演示业务价值，在 15 分钟内讲清架构、取舍和指标。

### 1.1 默认时间预算

- 周一至周五：每天 1.5 小时。
- 周六：5 小时深度开发。
- 周日：2.5 小时评测、复盘和下周规划。
- 每周约 15 小时，一年约 750–800 小时。

### 1.2 每日工作的固定定义

后文表格中的 D1–D7 对应周一至周日：

| 日期标识 | 固定职责                     | 当天必须留下的证据     |
| -------- | ---------------------------- | ---------------------- |
| D1       | 需求、调研、接口和验收标准   | Issue、ADR 或设计草图  |
| D2       | 数据模型、接口或核心模块实现 | 可运行代码和单元测试   |
| D3       | 核心功能继续实现             | 可演示的 Happy Path    |
| D4       | 异常分支、边界条件、安全     | 失败测试或故障注入记录 |
| D5       | 测试、重构、文档、代码审查   | 通过 CI 的提交         |
| D6       | 集成、部署、压测或大实验     | 可运行的阶段版本       |
| D7       | 评测、指标、复盘、下周拆解   | 周报和指标变化         |

### 1.3 年度阶段划分

| 阶段     |   周数 | 目标产出                              |
| -------- | -----: | ------------------------------------- |
| 项目一   |  1–20 | 招投标文件智能审查与响应辅助平台 v1.0 |
| 项目二   | 21–40 | 软件需求与代码质量 Agent v1.0         |
| 联合强化 | 41–46 | 公网部署、开源贡献、文章、作品主页    |
| 求职冲刺 | 47–52 | 简历、面试题、模拟面试和定向投递      |

---

## 2. 面向 30K 岗位的能力矩阵

两个项目不是各做各的，而是共同覆盖以下能力。

| 能力域      | 项目一负责证明                          | 项目二负责证明                    |
| ----------- | --------------------------------------- | --------------------------------- |
| Python 后端 | FastAPI、异步任务、SSE、缓存、文件处理  | 沙箱任务、工具执行、并发编排      |
| 数据与存储  | RAGFlow、PostgreSQL、Redis、对象存储    | Git 数据、任务状态、审计数据      |
| RAG         | 版面感知切分、混合召回、重排、溯源      | 只在需要理解规范时作为辅助工具    |
| Agent       | 文档工作流、证据判断、人审闭环          | Supervisor、专业 Agent、工具闭环  |
| LangGraph   | 长文档状态机、条件分支、Checkpoint      | 并行子图、HITL、长任务恢复        |
| 软件工程    | API、任务队列、测试、部署、监控         | AST、测试工具链、代码审查、PR     |
| 安全        | 文档注入、租户权限、敏感信息处理        | 命令白名单、沙箱、密钥和 Git 安全 |
| 评测        | 抽取 F1、Recall@K、风险召回、引用正确率 | 缺陷召回、测试质量、修复成功率    |
| 产品能力    | 招标解析、响应矩阵、投标合规审查        | 软件研发质量与协作效率            |

### 2.1 两个项目共同的最低工程标准

- Python 3.13+，完整类型标注。
- FastAPI + Pydantic，统一错误码与请求追踪 ID。
- PostgreSQL 保存业务状态，Redis 用于缓存、限流或任务状态。
- Ruff、Mypy、Pytest、Coverage、Pre-commit。
- Docker Compose 一键启动，提供 `.env.example`，禁止提交真实密钥。
- GitHub Actions 执行格式检查、类型检查、测试和依赖扫描。
- 结构化日志、Trace、Token/成本、P50/P95 延迟和错误率。
- 单元测试覆盖率目标不低于 70%，核心安全模块目标不低于 90%。
- 每项关键设计都要有 ADR，至少记录背景、方案、取舍和后果。
- 每个项目都要有 Baseline、增强方案、消融实验和失败案例。

---

# 项目一：招投标文件智能审查与响应辅助平台

## 3. 项目定位

项目名：**BidGuard**。

BidGuard 面向 IT、软件和网络服务类政府采购场景。它不是“上传招标 PDF 后聊天”，也不承诺替代律师、评标专家或投标负责人，而是把长篇招标文件转换为可核验的要求清单，并对投标响应进行有证据的完整性与一致性审查，重点解决：

- 招标文件篇幅长、格式复杂，资格条件、废标项和评分项容易漏读。
- 技术参数、商务条款和交付要求散落在正文、表格、附件及更正公告中。
- 投标响应与招标要求难以逐项对应，人工制作响应矩阵耗时且容易漏项。
- 同一承诺可能在技术方案、报价和服务条款中前后矛盾。
- 审查结论必须回到原文页码、章节和表格单元格，不能只给模型判断。
- 系统必须区分确定性规则、模型判断与人工确认，并可复现、可审计、可评测。

### 3.1 目标用户

- 投标经理：管理资格材料、废标风险和最终提交前检查。
- 售前/解决方案工程师：拆解技术要求、组织方案证据和响应内容。
- 商务与交付负责人：检查报价、工期、验收、服务等级等承诺的一致性。
- 内部复核人员：查看风险结论、来源证据、处理状态和审计记录。

### 3.2 非目标

- 不自动提交投标、不代签、不生成或伪造资质、业绩、印章和承诺材料。
- 不预测中标概率，不把模型结论包装成法律意见或最终废标判断。
- 不承诺获取非公开的完整供应商投标文件。
- 第一版不覆盖工程、药品、医疗器械等所有行业，只聚焦 IT/软件/网络服务采购。
- 不用“LLM 看起来合理”代替人工标注、规则校验和可复现评测。
- 不为了使用 Agent 而拆成多个角色；项目重点是文档智能、检索质量和审查可靠性。

## 4. 总体架构

```text
React / Next.js Web
        │
        ▼
FastAPI Gateway ── JWT / RBAC / Rate Limit / SSE
        │
        ├── Document Ingestion Workflow
        │     文件校验 → OCR/版面解析 → 章节/表格恢复 → Clause 抽取 → 人工复核
        │
        └── LangGraph Review Workflow
              1. 项目与文件分类
              2. 资格/废标/技术/商务/评分项抽取
              3. 要求标准化与响应矩阵生成
              4. 投标响应证据检索
              5. 确定性规则检查
              6. LLM 语义一致性判断
              7. 证据与引用校验
              8. 人工确认 / 驳回 / 修订
              9. 报告生成与审计留痕
        │
        ├── RAGFlow：解析基线、Embedding、混合检索、重排
        ├── PostgreSQL：项目、文件、条款、响应、风险、评测、审计
        ├── 对象存储：原始文件、解析产物、报告
        ├── Redis：缓存、限流、异步任务状态
        └── Observability：日志、Trace、Token、成本、延迟和失败分类
```

## 5. 必须实现的功能

### 5.1 数据采集与文档治理

- 维护数据清单：来源 URL、发布机构、公告日期、采集日期、文件哈希、文档类型、公开状态和再分发判断。
- 支持 PDF、扫描 PDF、DOCX、XLSX、ZIP 附件及更正公告的登记和关联。
- 原始文件不可覆盖；解析器、Prompt 和模型版本必须随结果记录。
- 文件去重、病毒/压缩包安全检查、大小限制、失败重试和隔离区。
- 权利状态不清晰的数据只保存来源和本地研究副本，不直接随公开仓库再分发。

### 5.2 版面解析与条款抽取

- 恢复标题层级、页码、段落、表格、列表编号和附件关系。
- OCR 仅作为扫描件回退路径，并记录页级置信度。
- 抽取资格条件、实质性/废标项、技术参数、商务条款、评分项和交付验收要求。
- 标准化 `shall/must/应/须/不得/★` 等强制性表达，但保留原文和位置。
- 每个结构化字段均能回到页码、章节、段落或表格单元格。

### 5.3 响应矩阵与版本差异

- 自动生成“要求—响应—证据—状态—负责人”矩阵。
- 支持人工拆分、合并、修正条款，并保留修改历史。
- 将更正公告与原招标文件对齐，识别新增、删除、数值变化和截止时间变化。
- 对要求设置 `未响应/部分响应/已响应/需人工确认/不适用` 状态。
- 导出 XLSX/PDF 报告，导出内容与系统状态一致且可追踪。

### 5.4 投标响应审查

- 从投标响应中检索每条要求对应的证据，不以整篇摘要代替逐项核对。
- 确定性检查优先处理日期、金额、数量、单位、附件缺失和必填字段。
- LLM 处理同义表达、条件承诺、范围差异和跨章节矛盾，并输出置信度与理由。
- 检出漏项、部分响应、无证据声明、技术参数不符、前后矛盾和可能过期材料。
- 高风险结论必须人工确认；系统不自动判定最终废标。

### 5.5 检索与可靠性

- 封装 RAGFlow Retrieve Chunks API，建立 BM25、向量、混合召回和重排对照组。
- 采用版面感知 Chunk；表格不得被无意义拆散，父子章节关系可用于上下文补全。
- 根据资格、参数、评分和合同问题动态选择过滤条件、`top_k` 与重排策略。
- Context Packer 在 Token 预算内保留原文、层级、页码和表头。
- Citation Verifier 检查每条风险结论是否由引用证据支持；证据不足时返回“需人工确认”。

### 5.6 工程化与安全

- FastAPI 异步任务接口、状态查询、SSE 进度和可取消任务。
- LangGraph Checkpoint 支持长文档任务失败恢复和人工复核中断。
- JWT、RBAC、项目级隔离；缓存 Key 必须包含租户、项目、权限和文件版本。
- 防御文件名攻击、Zip Slip、恶意宏、Prompt Injection 和跨项目越权检索。
- 管理端展示条款、检索证据、规则命中、模型判断、人工决定、耗时和失败原因。

## 6. 数据集与评测设计

### 6.1 数据分层与边界

| 数据层            | 内容                                     | 真实性与用途                                                  |
| ----------------- | ---------------------------------------- | ------------------------------------------------------------- |
| A：真实公开招标侧 | 招标公告、招标文件、技术附件、更正公告   | 真实数据，用于解析、抽取、检索和版本差异                      |
| B：真实公开结果侧 | 中标公告、公开报价表、中小企业声明等附件 | 真实数据，用于结果关联、报价结构和案例分析                    |
| C：受控响应侧     | 依据真实招标要求构造的投标响应样本       | 明确标记为合成，用于植入漏项、矛盾和参数偏差并建立 Gold Label |

第一阶段收集 20 个真实项目、约 80–150 份文件；最终目标为 50 个项目。公开仓库默认发布数据清单、下载脚本、标注和少量允许再分发的样例，不默认打包转载全部原文件。

### 6.2 Benchmark 最低要求

- 至少 200 条人工复核条款，覆盖资格、废标、技术、商务、评分和交付验收。
- 至少 100 条检索问题，每题保存 Gold Chunk、页码、章节和允许的答案变体。
- 至少 40 个响应审查案例，其中包含无缺陷对照组和受控注入缺陷组。
- 缺陷至少覆盖：漏项、部分响应、数值不符、单位不符、无证据承诺、前后矛盾、材料日期风险。
- 数据按项目划分训练/开发/测试集，禁止同一项目的相似条款跨集合泄漏。
- 每个合成样本记录生成方法、植入缺陷、Gold Label 和人工复核人，禁止与真实投标文件混淆。

### 6.3 验收指标

| 指标                         | v1.0 目标 |
| ---------------------------- | --------: |
| 关键条款抽取 Recall          |    ≥ 95% |
| 条款分类 Macro-F1            |    ≥ 88% |
| Gold Evidence Recall@5       |    ≥ 90% |
| Gold Evidence MRR            |   ≥ 0.80 |
| 风险检出 Recall              |    ≥ 90% |
| 风险检出 Precision           |    ≥ 80% |
| 引用正确率                   |    ≥ 95% |
| 关键数值规则准确率           |    ≥ 98% |
| 越权检索与恶意压缩包成功次数 |         0 |
| API 单元测试覆盖率           |    ≥ 70% |
| 核心安全模块覆盖率           |    ≥ 90% |

指标是挑战目标而非宣传数字。未达标时必须发布真实结果、失败类别、对照实验和下一步，不允许挑选样本或修改口径掩盖问题。

## 7. 项目一 20 周逐日计划

| 周 | D1                | D2              | D3                | D4              | D5                | D6               | D7             |
| -- | ----------------- | --------------- | ----------------- | --------------- | ----------------- | ---------------- | -------------- |
| 1  | ~~写问题陈述~~   | 定用户与场景    | 画业务流程        | 定系统边界      | 定成功指标        | 建 Issue/看板    | 评审章程 v0.1  |
| 2  | 建来源清单        | 定 Manifest     | 下载首批文件      | 哈希与去重      | 检查再分发边界    | 整理 10 个项目   | 数据审计 v0    |
| 3  | 研究 RAGFlow 解析 | 建解析 Baseline | PDF/DOCX 文本提取 | 扫描件 OCR 回退 | 错误与重试        | 跑首批文件       | 解析失败分类   |
| 4  | 定版面模型        | 恢复章节层级    | 表格结构恢复      | 附件关系        | 页码坐标映射      | 对比两种解析方案 | 版面评测 v0    |
| 5  | 定 Clause Schema  | 写标注规范      | 标注 50 条        | 双人复核样例    | 一致性检查        | 补至 100 条      | 冻结规范 v1    |
| 6  | 建 BM25 Baseline  | 接向量检索      | 接混合召回        | 接 Rerank       | 实现 Recall/MRR   | 跑对照实验       | 检索报告 v0    |
| 7  | 抽取资格条件      | 抽取废标项      | 抽取技术参数      | 抽取商务条款    | 抽取评分项        | 端到端抽取       | 错误分类       |
| 8  | 设计复核 UI       | 条款拆分/合并   | 原文定位          | 人工修订历史    | 审计日志          | 标注效率测试     | 发布抽取 v0.1  |
| 9  | 标准化评分项      | 分值校验        | 评分公式解析      | 强制项规则      | 数值/单位规则     | 复杂表格联调     | 规则测试报告   |
| 10 | 定响应矩阵模型    | 生成矩阵        | 状态流转          | 负责人字段      | XLSX 导出         | 完整场景联调     | 矩阵 v0.1      |
| 11 | 导入响应文件      | 证据检索        | 要求-证据匹配     | 低置信度分支    | 引用校验          | 跑 10 个案例     | 匹配评测 v0    |
| 12 | 漏项检测          | 部分响应检测    | 数值不符          | 前后矛盾        | 材料日期风险      | 组合缺陷测试     | 风险报告 v0    |
| 13 | 画 LangGraph 状态 | 定 State 类型   | 写解析子图        | 写审查子图      | HITL 中断         | Checkpoint 恢复  | 导出流程图/ADR |
| 14 | Query Rewrite     | 结构化过滤      | Context Packer    | Evidence Grader | Citation Verifier | 消融实验         | 决定默认策略   |
| 15 | 关联更正公告      | 章节对齐        | 文本差异          | 数值差异        | 截止时间规则      | 版本场景联调     | 差异报告 v0    |
| 16 | 补齐 200 条款     | 补齐 100 问题   | 构造审查样本      | 人工复核 Gold   | 防数据泄漏        | 冻结测试集       | Benchmark v1   |
| 17 | 定 OpenAPI        | 异步上传/解析   | 任务状态/SSE      | 取消与重试      | API 集成测试      | 前后端联调       | 接口文档 v1    |
| 18 | JWT/RBAC          | 项目隔离        | 文件安全          | 文档注入测试    | Trace/成本        | 故障注入与压测   | 安全性能报告   |
| 19 | 项目工作台        | 条款复核页      | 响应矩阵页        | 风险证据页      | E2E 测试          | 3 名用户试用     | 问题分级       |
| 20 | 修 P0/P1          | 跑最终 Baseline | 跑完整消融        | README/ADR      | Docker/CI/部署    | 录 Demo/发 v1.0  | 复盘与简历草稿 |

## 8. 项目一完成定义（Definition of Done）

只有同时满足以下条件，项目一才算完成：

- 新机器按照 README 能在 30 分钟内启动，示例数据可独立跑通。
- 数据清单包含至少 50 个真实公开项目，来源、哈希、类型和公开边界完整。
- 200 条 Gold Clause、100 条检索问题和 40 个审查案例可复现。
- 报告包含解析、抽取、检索、审查 Baseline，增强方案、消融结果和至少 15 个失败案例。
- 能现场展示条款抽取、原文定位、响应矩阵、漏项/矛盾审查、更正公告差异和人工确认。
- 每条高风险结论包含原文证据；证据不足时明确返回“需人工确认”。
- 公网 Demo 不包含来源不明的投标书、敏感信息或真实密钥。
- 能解释 RAGFlow 负责什么、自研工作流负责什么，以及规则与 LLM 的职责边界。
- 能明确说明真实数据、公开结果和受控合成响应的边界，不夸大数据真实性。

---

# 项目二：软件需求与代码质量 Agent

## 9. 项目定位

项目暂定名：**SpecGuard**。

SpecGuard 不是“输入一句话，模型自动写完整项目”的 Coding Agent。它面向软件团队中更可验证、更有工程价值的链路：

```text
需求 / GitHub Issue / Pull Request
    ↓
需求澄清与验收标准
    ↓
代码库结构与影响范围分析
    ↓
测试计划与测试生成
    ↓
代码质量、安全与回归风险审查
    ↓
受控修复（可选）
    ↓
人工审批
    ↓
创建分支、提交或PR
```

它必须证明的不是“模型会写代码”，而是：

- 能把模糊需求变成可追踪的验收标准。
- 能正确选择代码搜索、AST、测试、Lint 和安全扫描工具。
- 能用确定性工具验证 LLM 的判断。
- 能在沙箱中修改代码，并限制无关改动。
- 能暂停等待人工批准，恢复后继续执行。
- 能量化比较静态工具、单 Agent 和多 Agent 的效果与成本。

## 10. 适用场景

### 10.1 需求影响分析

输入一个 Issue，输出：

- 需求摘要、缺失信息和澄清问题。
- Given/When/Then 验收标准。
- 可能受影响的模块、接口、数据表和测试。
- 风险、依赖和建议实施顺序。

### 10.2 PR 代码审查

输入一个 PR Diff，输出：

- 正确性、边界条件、并发、性能、安全和可维护性问题。
- 每个问题的文件、行号、证据、严重度和修复建议。
- 区分“确定性工具发现”和“LLM推断”，降低幻觉式评论。

### 10.3 测试生成

- 根据验收标准生成单元测试和集成测试。
- 运行测试，修复测试自身的语法或导入错误。
- 使用覆盖率和 Mutation Testing 判断测试是否真正有效。

### 10.4 受控修复

- 只处理范围明确的小型 Bug 或质量问题。
- 在临时 Git 分支和隔离工作区修改。
- 通过测试、Lint、类型检查和安全扫描后才能请求人工批准。
- 没有明确批准不得 commit、push 或创建 PR。

## 11. 多 Agent 架构

```text
Supervisor / Workflow Controller
│
├── Requirement Agent
│   ├── 需求拆解
│   ├── 澄清问题
│   └── 验收标准与追踪矩阵
│
├── Repository Analysis Agent
│   ├── 目录与依赖分析
│   ├── 符号/引用搜索
│   └── 影响范围与上下文压缩
│
├── Test Agent
│   ├── 测试计划
│   ├── 测试生成
│   ├── pytest/coverage
│   └── mutation testing
│
└── Review & Verification Agent
    ├── Diff审查
    ├── Lint/类型/安全结果整合
    ├── 证据核验
    └── 受控修复建议
```

### 11.1 多 Agent 设计约束

- Agent 不自由聊天，通过 Pydantic 模型传递结构化结果。
- Supervisor 维护全局状态，子 Agent 默认无长期记忆。
- 每个子 Agent 只获得完成任务所需的最小上下文。
- Repository Agent 不直接改代码；Test Agent 不直接 push；Review Agent 不绕过审批。
- 能用确定性代码完成的步骤，不交给 LLM 自由判断。
- 同一任务最多两轮自我修正，禁止无限反思循环。

## 12. 工具体系

### 12.1 只读工具

- `list_files`：受限目录树。
- `search_code`：基于 `rg` 的搜索。
- `read_file_range`：限制文件和行数。
- `find_symbol` / `find_references`：AST 或语言服务分析。
- `git_diff` / `git_log` / `git_blame`。
- `read_project_config`：读取依赖、测试和Lint配置。

### 12.2 验证工具

- `run_pytest`。
- `run_coverage`。
- `run_ruff`。
- `run_mypy`。
- `run_bandit`。
- `run_dependency_audit`。
- `run_mutation_tests`。
- `build_project`。

### 12.3 写入工具

- `apply_patch_in_sandbox`。
- `create_test_file_in_sandbox`。
- `create_task_branch`。
- `create_commit`。
- `push_branch`。
- `create_pull_request`。

写入类工具必须分级授权。修改代码前展示计划；commit、push、PR 必须 Human-in-the-Loop。

## 13. 沙箱与安全边界

- 每个任务使用独立临时目录或容器，不直接修改主工作区。
- 命令必须来自白名单，不允许任意 Shell 字符串执行。
- 禁止读取 `.env`、SSH Key、浏览器数据、云凭据和工作区外文件。
- 默认禁网；需要下载依赖时使用显式允许的镜像和锁文件。
- CPU、内存、磁盘、进程数和执行时间必须有限制。
- 对删除、覆盖、依赖升级、大范围格式化设置硬限制。
- Diff 超过文件数或行数阈值时自动停止并请求人工确认。
- Agent 生成内容、工具参数、输出和审批结果全部写入审计日志。
- PR 分支不得直接绕过主分支保护规则。

## 14. 状态模型与执行图

核心 State 至少包含：

```python
class CodingTaskState(TypedDict):
    task_id: str
    repository: str
    base_commit: str
    user_request: str
    clarified_requirements: list[str]
    acceptance_criteria: list[str]
    impacted_files: list[str]
    evidence: list[dict]
    test_plan: list[dict]
    review_findings: list[dict]
    proposed_patch: str | None
    tool_events: list[dict]
    verification_results: dict
    risk_level: str
    approval_status: str
    retry_count: int
```

执行图必须包含：

- 缺少信息 → 中断并向用户澄清。
- 只需分析 → 不进入写入流程。
- 需要测试 → Test Agent。
- 需要审查 → Review Agent。
- 允许小型修复 → 沙箱 Patch。
- 验证失败 → 最多修正两次。
- 高风险或大 Diff → 人工批准。
- 批准 → commit/PR；拒绝 → 保留报告并结束。
- 中途进程崩溃 → 从 Checkpoint 恢复。

## 15. Benchmark 与评测设计

### 15.1 评测仓库

- 自建 3 个中小型 Python 服务作为可控基准。
- 至少覆盖 FastAPI、数据访问、缓存、后台任务和通用工具模块。
- 选择 1–2 个许可证允许的开源 Python 项目做外部验证。
- 固定 Commit SHA，避免上游变化导致结果无法复现。

### 15.2 至少 100 个任务

| 任务类型           | 最低数量 |
| ------------------ | -------: |
| 需求澄清与影响分析 |       20 |
| 已知Bug定位        |       20 |
| PR缺陷审查         |       25 |
| 测试生成           |       20 |
| 小范围受控修复     |       15 |

缺陷类型至少覆盖：空值、边界值、错误异常处理、资源泄漏、权限、注入、并发、缓存一致性、事务、API兼容性和性能退化。

### 15.3 Gold Label

每个任务保存：

- 需求和验收标准。
- 正确影响文件和关键符号。
- 预期发现的问题及严重度。
- 能暴露问题的测试。
- 允许修改的文件范围。
- 参考 Patch 或正确结果。
- 禁止执行的行为。

### 15.4 对照组

1. **Static Baseline**：只运行 Ruff、Mypy、Bandit、Pytest。
2. **Single Agent Baseline**：一个 Agent 拥有全部工具。
3. **Multi-Agent**：Supervisor + 专业 Agent + 确定性验证门禁。

项目必须回答：多 Agent 是否提高了准确率和证据质量，增加了多少延迟与成本。

### 15.5 验收指标

| 指标                         |      v1.0 目标 |
| ---------------------------- | -------------: |
| 需求验收标准覆盖率           |         ≥ 90% |
| 影响文件 Recall@5            |         ≥ 80% |
| 植入缺陷召回率               |         ≥ 80% |
| 审查问题 Precision           |         ≥ 70% |
| 生成测试可执行率             |         ≥ 95% |
| Mutation Score 相对提升      | ≥ 20 个百分点 |
| 小型任务 Patch 成功率        |         ≥ 60% |
| 修复后原有测试通过率         |           100% |
| 危险命令/越权读取拦截率      |           100% |
| 未批准的 commit/push/PR 次数 |              0 |

Patch 成功必须同时满足：应用成功、新增测试通过、原测试不回归、Lint/类型/安全检查通过、修改范围符合约束。

## 16. 项目二 20 周逐日计划

| 周 | D1                 | D2                  | D3                  | D4             | D5              | D6                     | D7                     |
| -- | ------------------ | ------------------- | ------------------- | -------------- | --------------- | ---------------------- | ---------------------- |
| 21 | 写产品问题陈述     | 定四类用例          | 定非目标            | 画安全边界     | 选评测仓库      | 建新仓库与看板         | 需求评审v0.1           |
| 22 | 设计Tool协议       | 实现Tool Registry   | 写结构化结果模型    | 超时与重试     | 工具单测        | FastAPI任务接口        | 工具Demo               |
| 23 | 设计沙箱           | 临时工作区          | 命令白名单          | 资源限制       | 安全测试        | 容器执行Demo           | 写威胁模型             |
| 24 | 实现文件列表       | 实现rg搜索          | 文件范围读取        | Git Diff/Log   | 只读越权测试    | 组合Repo工具           | 记录工具性能           |
| 25 | AST符号索引        | 引用搜索            | 依赖图              | 代码摘要缓存   | 索引测试        | 三仓库索引             | 评估上下文压缩         |
| 26 | 定需求输出Schema   | 写Requirement Agent | 澄清问题            | 验收标准       | 结构化输出测试  | 20个需求任务           | 分析错误类别           |
| 27 | 定影响分析Schema   | 写Repo Agent        | 文件候选排序        | 证据行号       | 幻觉文件测试    | 影响分析评测           | 输出Recall@5           |
| 28 | 接pytest           | 接coverage          | 接Ruff/Mypy         | 接Bandit/Audit | 工具异常测试    | 统一验证报告           | 发布工具链v0.1         |
| 29 | 定测试计划Schema   | 写Test Agent        | 生成单元测试        | 修复测试语法   | 测试写入沙箱    | 跑20个任务             | 统计可执行率           |
| 30 | 接Mutation Testing | 生成边界测试        | 生成异常测试        | 限制脆弱断言   | 测试质量单测    | 对比Coverage与Mutation | 写测试质量报告         |
| 31 | 定Review Finding   | 写Review Agent      | 整合静态工具        | 行号与证据核验 | 虚假评论测试    | 跑25个PR任务           | 统计Precision/Recall   |
| 32 | 设计全局State      | 写Supervisor        | 条件路由            | 并行只读调查   | 子Agent输出裁剪 | 端到端分析任务         | 导出执行图             |
| 33 | 实现Single Agent   | 跑相同任务集        | 实现Multi-Agent     | 统一Token统计  | 统一延迟统计    | 首次对照实验           | 写多Agent ADR          |
| 34 | 设计Patch策略      | 实现受限Patch       | Diff大小限制        | 验证失败回路   | Patch安全测试   | 跑10个修复任务         | 分析失败Patch          |
| 35 | 扩充15个修复任务   | 最多两轮修正        | 无关改动检测        | 回归测试门禁   | Patch质量测试   | 跑完整修复集           | 统计成功率             |
| 36 | 设计审批状态       | LangGraph Interrupt | approve/edit/reject | Checkpoint恢复 | HITL测试        | 中断恢复Demo           | 写审批ADR              |
| 37 | 创建任务分支       | 受控commit          | 受控push            | 创建Draft PR   | GitHub失败处理  | 完整PR闭环             | 审计外部副作用         |
| 38 | 结构化审计日志     | Trace Agent节点     | Trace工具调用       | 成本与延迟     | 可观测测试      | 故障注入恢复           | 性能与成本报告         |
| 39 | 补齐100任务        | 冻结Benchmark       | 跑Static            | 跑Single Agent | 跑Multi-Agent   | 做消融实验             | 输出最终评测报告       |
| 40 | 管理界面           | Diff与证据UI        | 审批界面            | 任务时间线     | E2E与安全测试   | Docker/CI/Demo/v1.0    | README、复盘、简历草稿 |

## 17. 项目二完成定义（Definition of Done）

- 能演示 Issue → 需求澄清 → 影响分析 → 测试 → 审查 → 审批 → Draft PR。
- 全流程在隔离环境执行，主工作区和默认分支不会被自动修改。
- 100 个任务 Benchmark 可复现，并有三组对照实验。
- 每条审查问题包含文件、行号、证据、严重度和验证来源。
- 能展示一个任务在中途失败后从 Checkpoint 恢复。
- 能展示高风险操作被阻止、被拒绝和被编辑后恢复执行。
- 能解释为什么使用多 Agent，以及哪些步骤刻意保持确定性。

---

# 联合强化与求职阶段

## 18. 第 41–46 周：两个项目联合强化

| 周 | D1            | D2           | D3            | D4              | D5             | D6             | D7           |
| -- | ------------- | ------------ | ------------- | --------------- | -------------- | -------------- | ------------ |
| 41 | 统一错误规范  | 统一日志字段 | 统一配置管理  | 统一鉴权        | 共用模板测试   | 跨项目代码审查 | 工程规范v1.0 |
| 42 | 选云部署方案  | 部署BidGuard | 部署SpecGuard | HTTPS与域名     | 备份恢复       | 线上故障演练   | 部署报告     |
| 43 | 招募3名试用者 | 收项目一反馈 | 收项目二反馈  | 问题分级        | 修P0           | 修P1           | 发布v1.1     |
| 44 | 选开源Issue   | 复现问题     | 提交修复      | 补测试          | 写英文说明     | 提交PR         | 记录开源贡献 |
| 45 | 写RAG评测文章 | 补实验图     | 发布文章一    | 写Agent安全文章 | 补消融图       | 发布文章二     | 整理读者问题 |
| 46 | 建作品主页    | 放架构与指标 | 放在线Demo    | 放演示视频      | 优化README首页 | 模拟现场演示   | 作品集v1.0   |

## 19. 第 47–52 周：求职冲刺

| 周 | D1             | D2           | D3           | D4           | D5              | D6             | D7             |
| -- | -------------- | ------------ | ------------ | ------------ | --------------- | -------------- | -------------- |
| 47 | 整理工作成果   | 写STAR案例1  | 写STAR案例2  | 项目量化描述 | 一页简历        | 找人Review     | 修订简历       |
| 48 | Python并发     | 类型与内存   | FastAPI原理  | HTTP与网络   | Redis与缓存     | 手写代码模拟   | 错题复盘       |
| 49 | PostgreSQL索引 | 事务隔离     | SQL优化      | 分布式一致性 | 消息与重试      | 系统设计模拟   | 错题复盘       |
| 50 | Embedding/BM25 | 向量索引     | Rerank       | RAG评测      | Agent状态与工具 | AI系统设计模拟 | 错题复盘       |
| 51 | 5分钟项目一    | 15分钟项目一 | 5分钟项目二  | 15分钟项目二 | 行为面试        | 两次完整Mock   | 按反馈修正     |
| 52 | 收集30个JD     | 建能力矩阵   | 定制三版简历 | 首批投递     | 面试跟踪表      | 复盘与补缺     | 制定下一月节奏 |

---

## 20. 每周复盘模板

每周日必须在 `docs/weekly/YYYY-Www.md` 记录：

```markdown
# Week N

## 本周承诺
- [ ] ...

## 实际交付
- Commit / PR：
- Demo：
- 测试：
- 指标：

## 本周失败
- 失败现象：
- 根因：
- 已验证的证据：
- 下周调整：

## 时间与成本
- 实际投入小时：
- LLM调用成本：
- 最大时间消耗点：

## 下周三个最重要目标
1. ...
2. ...
3. ...
```

## 21. 项目质量门禁

### 21.1 每个功能进入主分支前

- 有明确 Issue 和验收标准。
- 有单元测试或说明为何无法自动测试。
- Ruff、Mypy、Pytest 全部通过。
- 没有真实密钥、个人数据和大文件。
- README 或接口文档同步更新。
- 对新增 LLM 节点记录输入、输出、失败和成本。

### 21.2 每个项目发布前

- 新环境一键部署验证通过。
- Benchmark 在干净环境重跑成功。
- Demo 不依赖开发者手工修改数据库。
- 失败场景、权限和安全测试通过。
- 线上监控、备份和恢复流程至少演练一次。
- 视频中展示真实运行，不使用静态假页面冒充结果。

## 22. 防止项目失控的范围规则

- UI 最多投入总时间的 15%，优先保证核心链路和评测。
- 没有 Baseline 前不做高级优化。
- 没有评测证明前，不因为“听起来高级”引入 GraphRAG、多 Agent 或微调。
- 单个 Agent 工具不超过其职责需要，避免所有 Agent 拿到全部权限。
- 不同时支持五种模型厂商；先抽象接口，再稳定支持两种。
- 不追求自研向量数据库、模型训练或完整 IDE，这些不是项目目标。
- 每两周砍掉没有指标或演示价值的需求。

## 23. 最终作品包

每个项目都必须具备：

- 公开 GitHub 仓库和规范提交历史。
- 清晰 README、架构图、状态图和时序图。
- `docs/adr/` 技术决策记录。
- `benchmark/` 数据、标注规范和评测脚本。
- `tests/` 单元、集成、安全和 E2E 测试。
- Docker Compose 和 GitHub Actions。
- 在线 Demo、3–5 分钟视频和 15 分钟讲解稿。
- Baseline、消融实验、成本延迟和失败案例报告。
- 一篇技术文章和一段不夸大的简历描述。

## 24. 简历描述模板

数字必须替换为真实评测结果。

### 24.1 BidGuard

> 独立设计并实现基于 RAGFlow、LangGraph 和 FastAPI 的招投标文件智能审查平台，完成复杂 PDF/表格解析、资格与评分条款抽取、响应矩阵生成、投标响应证据匹配和风险审查；构建包含 `[N]` 个真实公开采购项目、`[M]` 条人工标注条款和 `[K]` 个受控审查案例的 Benchmark，通过混合召回、重排与确定性规则将 Gold Evidence Recall@5 从 `[A]` 提升至 `[B]`、风险检出 Recall 达到 `[C]`，并以页码级证据、HITL、RBAC、Checkpoint、Docker 和 CI/CD 实现可审计、可恢复部署。

### 24.2 SpecGuard

> 独立实现面向需求分析、测试生成和代码审查的多 Agent 平台，基于 LangGraph 编排 Requirement、Repository、Test 和 Review Agent，集成 AST、Git、Pytest、Ruff、Mypy、Bandit 和 Mutation Testing 等确定性工具；设计沙箱、命令白名单、HITL审批和任务恢复机制，在 `[N]` 个基准任务上实现缺陷召回率 `[A]`、审查Precision `[B]`、Patch成功率 `[C]`，并对Static、Single-Agent和Multi-Agent方案完成成本与效果对照。

## 25. 面试时必须能回答的问题

### BidGuard

1. 真实招标数据从哪里获得，公开访问是否等于可以在 GitHub 再分发？
2. 如何区分真实招标文件、公开中标附件和受控合成投标响应？
3. RAGFlow 负责哪些能力，你实现了哪些版面解析、条款模型和审查工作流？
4. 为什么资格/废标条款更看重 Recall，如何控制低 Precision 带来的人工成本？
5. 表格、跨页条款和更正公告为何会导致普通 Chunking 失败？
6. 哪些审查适合确定性规则，哪些需要 LLM，冲突时如何处理？
7. 如何标注 Gold Evidence，Recall@K、MRR、风险 Precision/Recall 如何计算？
8. 如何防止恶意文档注入、Zip Slip、跨项目检索和敏感文件泄漏？
9. 为什么高风险结论必须 HITL，而不能让 Agent 自动决定废标？
10. 系统最常见的三个失败模式是什么，消融实验说明了什么？

### SpecGuard

1. 为什么要多 Agent，单 Agent 的问题在哪里？
2. 哪些步骤必须确定性执行，哪些适合 LLM？
3. 如何限制 Agent 读取密钥或执行危险命令？
4. 如何评价代码审查的 Precision 和 Recall？
5. Coverage 高为什么不等于测试质量高？
6. 如何防止修复一个 Bug 引入新的回归？
7. LangGraph 中断、Checkpoint 和恢复如何工作？
8. 如果多 Agent 更贵但效果提升不明显，你会如何简化？

---

## 26. 参考资料与岗位校准

- [RAGFlow 官方仓库](https://github.com/infiniflow/ragflow)
- [LangGraph Overview](https://docs.langchain.com/oss/python/langgraph/overview)
- [LangGraph Persistence](https://docs.langchain.com/oss/python/langgraph/persistence)
- [LangChain Human-in-the-Loop](https://docs.langchain.com/oss/python/langchain/human-in-the-loop)
- [LangChain Multi-agent Subagents](https://docs.langchain.com/oss/python/langchain/multi-agent/subagents)
- [LangChain Custom Workflow](https://docs.langchain.com/oss/python/langchain/multi-agent/custom-workflow)
- [Apple 上海 AI Application Engineer 岗位示例](https://jobs.apple.com/en-us/details/200652580-3715/fullstack-software-engineer?team=OPMFG)
- [AstraZeneca 上海 AI Engineer 岗位示例](https://job-search.astrazeneca.cn/%E5%B7%A5%E4%BD%9C/%E4%B8%8A%E6%B5%B7/ai-engineer-biometrics-and-data-science/12977/93765282048)

---

## 27. 第一天立即执行

从项目一第 1 周 D1 开始，只完成以下内容：

1. 创建任务：`Define BidGuard problem statement, scope and success metrics`。
2. 写清楚目标用户、三个核心业务场景、系统输入输出和明确非目标。
3. 确认首期只覆盖 IT/软件/网络服务类政府采购，并写明真实与合成数据边界。
4. 写出第一版业务与技术成功标准、风险和人工复核边界，不写功能代码。
5. 当天结束前提交 `docs/bidguard/day01_project_charter.md`，第二天再建立数据 Manifest 并采集首批样本。

先把问题定义正确，再写系统。真正能支撑 30K 面试的不是功能数量，而是完整的工程证据、可复现指标以及对失败和取舍的清晰解释。
